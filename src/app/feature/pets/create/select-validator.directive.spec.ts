import { SelectValidatorDirective } from './select-validator.directive';

describe('SelectValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
